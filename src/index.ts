export type ProjectSignal = { project: string; owner: string; profile: string };
export const signal: ProjectSignal = { project: "api-sketch-2do0", owner: "JohnHale910066", profile: "0037" };
export const headline = signal.project + " / " + signal.owner;
